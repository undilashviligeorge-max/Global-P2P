services:
  - type: web
    name: p2p-remittance-backend
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /health
    envVars:
      - key: TATUM_API_KEY
        sync: false
